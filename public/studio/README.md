Drop studio interior photos in this folder and list the filenames in
STUDIO_IMAGES in src/pages/Studio.jsx. Keep them roughly the same
aspect ratio (square works best) and under ~500kb each so the page
loads fast on mobile.
